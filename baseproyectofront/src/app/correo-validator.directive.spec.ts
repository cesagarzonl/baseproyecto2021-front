import { CorreoValidatorDirective } from './correo-validator.directive';

describe('CorreoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CorreoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
